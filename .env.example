# Better Auth
BETTER_AUTH_SECRET=           # genera con: npx -y @better-auth/cli secret
BETTER_AUTH_URL=http://localhost:3000

# BD local (Drizzle / better-sqlite3). En producción se usa el binding D1.
DATABASE_URL=file:./dev.db

# Sentry (opcional)
VITE_SENTRY_DSN=
