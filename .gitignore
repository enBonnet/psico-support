node_modules
.DS_Store
dist
dist-ssr
*.local
.env.sentry-build-plugin
.env
.env.local
.nitro
.tanstack
.wrangler
.output
.vinxi
__unconfig*
todos.json
dev.db
dev.db-bak
dev.db.bak
# WAL sidecars of the local runtime DB (dev.db runs in WAL mode)
dev.db-wal
dev.db-shm
# Local dev services (R2 filesystem bucket under .local/media)
.local/
# Playwright CLI session artifacts (console logs, page snapshots)
.playwright-cli/
.claude/
.agents/
.cursor/
.cursorrules

# Local export — PII
/professionals.csv
# db:pull-prod scratch dir (raw prod SQL — never commit)
.tmp/
# Local Pi runtime state
.atl/

# ZCode agent session plans (internal scratchpad)
.zcode/

#opencode agent and config
.opencode